<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SkyBuy.View.AdminLoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SkyBuy.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20" Background="DimGray">


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1">
            <Grid>
               
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

            
                <Border Grid.Row="0" Background="Bisque">
                    <StackPanel Orientation="Vertical">

                        <TextBlock FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Text="Login into admin portal using credentials" Margin="0,100,15,0"/>
                        
                        <TextBox
                         MaxLength="50"
                         Width="200"   
                         PlaceholderText="Enter your username"
                         Text="{x:Bind ViewModel.Username, Mode=TwoWay}" 
                         VerticalAlignment="Center" HorizontalAlignment="Center"  Margin="0,100,15,0"
                        />      
                        <PasswordBox 
                         Width="200" 
                         MaxLength="50"
                         PlaceholderText="Enter your password"
                         Password="{x:Bind ViewModel.Password, Mode=TwoWay}"
                         PasswordRevealMode="Peek"
                         VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,20,15,0" 
                        />
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Background="Plum">
                    <StackPanel Orientation="Vertical">
                        <ProgressRing Visibility="{x:Bind ViewModel.IsLoading,Mode=OneWay}" IsActive="{x:Bind ViewModel.IsLoading ,Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        <InfoBar MaxWidth="290" IsOpen="{x:Bind ViewModel.LoginBad ,Mode=OneWay}" Severity="Error" Message="{x:Bind ViewModel.ErrorMsg ,Mode=OneWay}"/>
                        <Button IsEnabled="{x:Bind ViewModel.IsNotLoading}" Content="LOGIN" Command="{x:Bind ViewModel.LoginFunctionCommand}" VerticalAlignment="Center"  HorizontalAlignment="Center" Margin="0,40,0,0"/>
                    </StackPanel>
                </Border>


            </Grid>
        </Border>
        
        
    </Grid>
</Page>
